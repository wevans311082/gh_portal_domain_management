(function () {
    // Homepage visuals are now static atmospheric gradients by design.
    // Keep this file as a stable hook for future homepage interactions.
    document.addEventListener("DOMContentLoaded", function () {});
})();
